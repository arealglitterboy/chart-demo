import React from "react";

const Navbar = (props) => (
    <nav className="navbar">
        <img src="" alt="Site Logo" className="navbar__logo" />
        <section className="navbar__links"></section>
        <section className="navbar__profile"></section>
    </nav>
);

export default Navbar;